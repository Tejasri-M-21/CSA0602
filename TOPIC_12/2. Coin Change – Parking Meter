#include <stdio.h>

int main() {
    int coins[] = {25, 10, 5, 1};
    int n = 4;
    int amount = 63;
    int count = 0;

    printf("Coins used: ");

    for (int i = 0; i < n; i++) {
        while (amount >= coins[i]) {
            printf("%d ", coins[i]);
            amount -= coins[i];
            count++;
        }
    }

    printf("\nMinimum coins = %d\n", count);

    return 0;
}
